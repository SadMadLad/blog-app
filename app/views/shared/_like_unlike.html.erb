<%= render "shared/unlike_button", like: is_liked(component) if is_liked(component) %>
<%= render "shared/like_button", likeable: {id: component.id, type: component_type} unless is_liked(component) %>
